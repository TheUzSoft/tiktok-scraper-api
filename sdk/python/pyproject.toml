[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tiktok-scraper-api-sdk"
version = "1.0.0"
description = "Official Python SDK wrapper for the TikTok Scraper API"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.28.0"
]
